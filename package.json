{
  "name": "pi-agent-web",
  "private": true,
  "packageManager": "pnpm@11.18.0",
  "engines": { "node": ">=24.12.0" },
  "scripts": {
    "build": "corepack pnpm --filter @pi-web/protocol build && corepack pnpm --filter @pi-web/web build && corepack pnpm --filter @pi-web/server build",
    "dev": "corepack pnpm --parallel --filter @pi-web/server --filter @pi-web/web dev",
    "start": "corepack pnpm --filter @pi-web/server start",
    "test": "corepack pnpm -r test",
    "typecheck": "corepack pnpm -r typecheck",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
