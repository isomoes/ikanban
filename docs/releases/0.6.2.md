# v0.6.2 verification

Date: 2026-09-22. Local toolchain: Bun 1.3.12 and Node v24.12.0.

## Automated release checks

| Check | Result |
| --- | --- |
| `bun install --frozen-lockfile` | Passed |
| `bun run typecheck` | Passed for all three workspaces |
| `bun run --cwd packages/web test:unit` | 925 passed, 1 skipped, 0 failed |
| `env -u VITE_OPENCODE_URL bun run build:web` | Passed |
| `bunx playwright install chromium` in `packages/web` | Passed |
| `env -u VITE_OPENCODE_URL bun run --cwd packages/web test:pages` | All 5 passed |
| `node scripts/check-release.mjs v0.6.2` | Passed |

Pages tests cover the initial connection screen without an implicit backend,
asset and manifest paths, the deep-link fallback, a Basic-auth fixture, and
service-worker scope/offline navigation. The repository's Pages source was
confirmed as GitHub Actions.

The root/web version is 0.6.2. Shared libraries remain at upstream 2.0.12,
pinned to `dcfe1ec7bd4922d4f44c141ba33047402bffc57e`. The version-bump command
regenerated `bun.lock` without a content change; Bun does not record versions
for the unreferenced root/web workspaces in this lockfile.

## Real-backend checks

No real-backend checks were performed for this release. No test backend URL or
password was supplied for this release run; the local API fixture is not an
OpenCode backend.

| Interaction | Result |
| --- | --- |
| Backend version | Not recorded; no live backend tested |
| URL/password connection | Untested against a real backend |
| Server switching | Untested against a real backend |
| Project/session loading | Untested against a real backend |
| Prompt submission and streamed response | Untested against a real backend |
| Permissions/forms | Untested against a real backend |
| Files/diffs | Untested against a real backend |
| Terminal WebSocket | Untested against a real backend |

The frontend source pin, successful build, and fixture tests do not establish
live-backend or provider compatibility.
